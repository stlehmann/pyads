language: python
python:
  - "2.7"
  - "3.5"

# command to install dependencies
install:
  - apt-get install -y git
  - git clone https://github.com/dabrowne/ADS.git
  - cd ADS
  - sudo make install
  - cd ..
  - pip install .
script: py.test